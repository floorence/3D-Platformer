refactor and clean up
- optimize getting length of vectors
- stop hard coding stuff
- optimize: FontTexture::generateIndices only needs size of vertices. and it will be the same every time till the size is reached
- DEPRECATE COLOR! think of a better way to apply color instead of/on top of texture.

feature
- way to only log some log levels
- test on different window aspect ratio
- collision
- gravity
- draw debug vectors
- third person perspective

fix
- disable depth buffer for gui drawing